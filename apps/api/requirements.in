# Development and optional-provider dependencies. Regenerate requirements.txt with:
# python -m piptools compile --generate-hashes --strip-extras --output-file requirements.txt requirements.in
# Keep this production block synchronized with requirements-prod.in. Dependabot's
# dependency-graph runner does not copy nested .in references into its temporary
# evaluation directory.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
cryptography==48.0.1
genanki==0.13.1
redis==6.4.0
python-multipart==0.0.32
edge-tts==7.2.8
pytest==9.1.1
httpx==0.28.1
PyYAML==6.0.3
ruff==0.16.6
